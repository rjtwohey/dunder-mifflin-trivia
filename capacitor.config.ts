import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dunder-mifflin-trivia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
